using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Noteblock : MonoBehaviour
{
    /*
    public Animator m_Animator;
    public bool notepadisActive = false;
    // Start is called before the first frame update
    void Start()
    {
        m_Animator.GetComponent<Animator>();
    }

    // Update is called once per frame
    void Update()
    {
        if (Input.GetKeyDown(KeyCode.Q) || Input.GetButtonDown("Notepad"))
        {
            m_Animator.SetBool("GoToFront", true);
            m_Animator.SetBool("GoToSide", false);
            notepadisActive = true;
        }

        if (Input.GetKeyDown(KeyCode.Q) || Input.GetButtonDown("Notepad"))
        {
            m_Animator.SetBool("GoToFront", false);
            m_Animator.SetBool("GoToSide", true);
            notepadisActive = false;
        }
    }
    */
}
